-- =====================================================================
--  STORM HUB -- MM2 PREMIUM ULTIMATE EDITION (WINDUI)
--  Author: Storming+AI
--  Revision: Merged v4 — Added AlignPosition, Invisibility, Silent Kills, Ignore Murderer Mode
-- =====================================================================

-- SERVICES
local Players           = game:GetService("Players")
local TweenService      = game:GetService("TweenService")
local RunService        = game:GetService("RunService")
local VirtualUser       = game:GetService("VirtualUser")
local Lighting          = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace         = game:GetService("Workspace")
local Stats             = game:GetService("Stats")
local TeleportService   = game:GetService("TeleportService")
local CoreGui           = game:GetService("CoreGui")
local GuiService        = game:GetService("GuiService")

-- WINDUI INITIALIZATION
local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "Storm Hub",
    Author = "MM2 Ultimate AFK Edition",
    Theme = "Dark",
    Folder = "StormHub_MM2_Config",
    Size = UDim2.fromOffset(650, 480),
    Transparency = 0.05,
    Acrylic = true
})

local function sendNotification(config)
    WindUI:Notify({
        Title   = config.Title or "Storm Hub",
        Content = config.Desc or "",
        Duration = config.Duration or 3
    })
end

-- =======================================================
--   QUEUE ON TELEPORT / AUTO-RELOAD ENGINE
-- =======================================================
local function queueScriptReload()
    local queueFunc = (syn and syn.queue_on_teleport)
        or queue_on_teleport
        or (Fluxus and Fluxus.queue_on_teleport)
        or (teleport and teleport.queue)

    if queueFunc then
        pcall(function()
            queueFunc([[
                repeat task.wait(1) until game:IsLoaded()
                task.wait(2)
                
                -- Directly load the remote script without checking for local files
                local success, err = pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/storming-acc/auto-farm/refs/heads/main/ai_studio_code%20(8).txt"))()
                end)

                if success then
                    print("Storm Hub Script reloaded successfully after teleport/rejoin!")
                else
                    warn("Storm Hub: Failed to reload script after teleport - " .. tostring(err))
                end
            ]])
        end)
    end
end

-- AUTO REJOIN ON KICK / DISCONNECT
pcall(function()
    local promptOverlay = CoreGui:WaitForChild("PromptOverlay", 5)
    if promptOverlay then
        promptOverlay.ChildAdded:Connect(function(child)
            if child.Name == "ErrorPrompt" then
                sendNotification({ Title = "Connection Lost", Desc = "Kick detected! Auto-rejoining in 3 seconds..." })
                task.wait(3)
                queueScriptReload()
                TeleportService:Teleport(game.PlaceId, Players.LocalPlayer)
            end
        end)
    end
end)

GuiService.ErrorMessageChanged:Connect(function()
    task.wait(2)
    queueScriptReload()
    TeleportService:Teleport(game.PlaceId, Players.LocalPlayer)
end)

-- =======================================================
--   REMOTES
-- =======================================================
local Remotes         = ReplicatedStorage:WaitForChild("Remotes")
local ExtrasRemotes   = Remotes:WaitForChild("Extras")
local GameplayRemotes = Remotes:WaitForChild("Gameplay")

local GetPlayerDataRemote  = ExtrasRemotes:WaitForChild("GetPlayerData")
local RoundStartRemote     = GameplayRemotes:WaitForChild("RoundStart")
local CoinsStartedRemote   = GameplayRemotes:WaitForChild("CoinsStarted")
local VictoryScreenRemote  = GameplayRemotes:WaitForChild("VictoryScreen")
local CoinCollectedEvent   = GameplayRemotes:WaitForChild("CoinCollected")
local OpenCrateRemote      = Remotes:WaitForChild("Shop"):WaitForChild("OpenCrate")

-- =======================================================
--   VARIABLES
-- =======================================================
local player    = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local Camera    = Workspace.CurrentCamera
local character = player.Character or player.CharacterAdded:Wait()
local rootPart  = character:WaitForChild("HumanoidRootPart")

local coinBlacklists     = {}
local isActive           = false
local isRoundActive      = false
local isSearchingForGun  = false
local autoGrabGun        = true
local antiLagEnabled     = false
local antiLagConnection  = nil
local farmMode           = "Lerp"   -- "Velocity (Glide)" | "Lerp" | "Instant TP" | "AlignPosition"
local ignoreMurderer     = false    -- NEW: Ignore Murderer mode
local flySpeed           = 24
local collected          = 0
local startTime          = 0
local antiAFK            = true
local originalGravity    = Workspace.Gravity
local maxCoinLimit       = 40
local debugTraces        = false

-- Invisibility State
local isInvis            = false
local invisConnection     = nil

-- Silent Combat State
local silentAuraEnabled  = false
local silentAuraRange    = 20
local silentAuraConnection = nil

-- AlignPosition Controllers
local farmAttachment     = nil
local alignPos           = nil
local alignRot           = nil

-- Rival detection
local detectRivalFarmers    = true
local detectedAutoFarmers   = {}
local playerLastPositions   = {}
local playerSpeedFrames     = {}
local avoidFarmerCoins      = true
local priorityKillFarmers   = true
local autoServerHopOnFarmers = false
local maxFarmersBeforeHop   = 2

-- Coords
local safeWaitingPlace = Vector3.new(9999, -300, 9999)

-- Crate opener
local autoOpenCrate   = true
local targetType      = "Coins"
local selectedCrates  = {}
local currentCrateIndex = 1
local crateOptions = {
    "KnifeBox1", "KnifeBox2", "KnifeBox3", "KnifeBox4", "KnifeBox5",
    "GunBox1",   "GunBox2",   "GunBox3",   "GunBox4",   "GunBox5",
    "MysteryBox1", "MysteryBox2", "RainbowBox"
}

-- Match state
local matchData         = nil
local localCoins        = 0
local bagsFull          = false
local hasResetForFullBag = false
local murdererPosition  = nil
local murdererPlayer    = nil

-- Kinematic prediction
local State = {
    Prediction  = 0.12,
    GunLeadMult = 1.1
}

-- Globals
Workspace.FallenPartsDestroyHeight = -50000
pcall(function()
    player.DevCameraOcclusionMode = Enum.DevCameraOcclusionMode.Invisicam
end)

-- =======================================================
--   FEATURE 2: CAMERA-OFFSET INVISIBILITY
-- =======================================================
local function toggleInvisible(state)
    isInvis = state
    if invisConnection then 
        invisConnection:Disconnect() 
        invisConnection = nil 
    end

    if state then
        invisConnection = RunService.Heartbeat:Connect(function()
            if rootPart and character and character:FindFirstChildOfClass("Humanoid") then
                local realCFrame = rootPart.CFrame
                local fakeCFrame = realCFrame * CFrame.new(0, -200000, 0)

                rootPart.CFrame = fakeCFrame
                character:FindFirstChildOfClass("Humanoid").CameraOffset = fakeCFrame:ToObjectSpace(CFrame.new(realCFrame.Position)).Position

                RunService.RenderStepped:Wait()
                if rootPart then 
                    rootPart.CFrame = realCFrame 
                end
            end
        end)
        sendNotification({ Title = "Invisibility Active", Desc = "Your character is hidden from other players." })
    else
        if character and character:FindFirstChildOfClass("Humanoid") then
            character:FindFirstChildOfClass("Humanoid").CameraOffset = Vector3.zero
        end
        sendNotification({ Title = "Invisibility Disabled", Desc = "Your character is visible again." })
    end
end

-- =======================================================
--   FEATURE 1: ALIGNPOSITION CONTROLLER HELPERS
-- =======================================================
local function cleanupAlignFarm()
    if alignPos then pcall(function() alignPos:Destroy() end) alignPos = nil end
    if alignRot then pcall(function() alignRot:Destroy() end) alignRot = nil end
    if farmAttachment then pcall(function() farmAttachment:Destroy() end) farmAttachment = nil end
end

local function moveToCoinAlign(targetPos)
    if not rootPart then return end

    if not farmAttachment or not farmAttachment.Parent then
        cleanupAlignFarm()
        farmAttachment = Instance.new("Attachment", rootPart)

        alignPos = Instance.new("AlignPosition")
        alignPos.Mode = Enum.PositionAlignmentMode.OneAttachment
        alignPos.Attachment0 = farmAttachment
        alignPos.RigidityEnabled = true
        alignPos.Position = rootPart.Position
        alignPos.Parent = rootPart

        alignRot = Instance.new("AlignOrientation")
        alignRot.Mode = Enum.OrientationAlignmentMode.OneAttachment
        alignRot.Attachment0 = farmAttachment
        alignRot.RigidityEnabled = true
        alignRot.CFrame = rootPart.CFrame * CFrame.Angles(math.rad(90), 0, 0)
        alignRot.Parent = rootPart
    end

    alignPos.Position = targetPos
end

-- =======================================================
--   SMART LOBBY DETECTION
-- =======================================================
local function isPlayerInLobby()
    local container = Workspace:FindFirstChild("CoinContainer", true)
        or Workspace:FindFirstChild("CoinVisuals", true)
        or Workspace:FindFirstChild("Coins", true)
    if not container then return true end

    local lobby = Workspace:FindFirstChild("Lobby")
    local char  = player.Character
    local hrp   = char and char:FindFirstChild("HumanoidRootPart")

    if lobby and hrp then
        if (hrp.Position - lobby:GetPivot().Position).Magnitude < 200 then
            return true
        end
    end
    return false
end

-- (file continues unchanged...)
